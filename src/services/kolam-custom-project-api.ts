/**
 * Compatibility re-export — Task Manager still imports this path.
 * Implementation lives in kolam-proyek-api.ts (P0).
 */
export {
  getKolamCustomProjectOptions,
  type KolamCustomProjectOption,
} from './kolam-proyek-api';
